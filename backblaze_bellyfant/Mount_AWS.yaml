# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: Mount Bellyfant Drive

  version: 1
  id: "ap::rclone::backblazemount"
  category: user
  type: python
  author: Matthäus Niedoba
  description: "Mounts a network drive to the Bellyfant Cloud Drive"
  icon:
    path: aws.svg

  script: "Mount_AWS.py"
  inputs:
    bucket_name: "Bellyfant"
    bucket_folder: "Bellyfant"
    server_name: "bellyfant"

  register:
    folder:
      enable: true
      filter: "*" 